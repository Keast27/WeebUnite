using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class Konami : Powerup
{
    // Start is called before the first frame update
    void Start()
    {
        name = "Konami Code";
        used = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public override void Use()
    {

    }
}
